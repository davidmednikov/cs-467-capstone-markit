import 'package:flutter/material.dart';

class AddList extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Navigating works, but we still need to add the back arrow to the scaffold / app bar."),
    );
  }
}